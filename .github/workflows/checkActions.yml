name: Aditya_Lenka_Actions
on: push
jobs:
    action-demo:
        runs-on: ubuntu-latest
        steps:
            - name: checkout the code
              uses: action/checkout@v4
            - name: print index.html file
              run: cat index.html
